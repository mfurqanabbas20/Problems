#include <iostream>
using namespace std;
void BubbleSort(int *arr, int size)
{
}
int main()
{
    int arr[] = {1, 3, 4, 5, 3, 4, 2, 3 102, 21};
    int size = sizeof(arr) / sizeof(arr[0]);
    BubbleSort(arr, size);
    return 0;
}